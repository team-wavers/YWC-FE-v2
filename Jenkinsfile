pipeline {
    agent any
    tools {
        nodejs "16.20.2"
    }

    stages {
        stage("Build") {
            steps {
                // git(
                //     url: "git remote origin",
                //     cre1tentialsId: "",
                //     branch: "main"
                // )

                // sh "yarn install"
                // sh "yarn build"
                sh "pwd"
            }
        }
    }
}