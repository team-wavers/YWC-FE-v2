pipeline {
    agent any
    // tools {
    //     nodejs ""
    // }

    stages {
        stage("Build") {
            steps {
                // git(
                //     url: "git remote origin",
                //     cretentialsId: "",
                //     branch: "main"
                // )

                // sh "yarn install"
                // sh "yarn build"
                sh "pwd"
            }
        }

        stage("Build - Development") {
            steps {
                // git(
                //     url: "git remote origin",
                //     crententialsId: "",
                //     branch: "develop"
                // )

                // sh "yarn install"
                // sh "yarn build"
            }
        }
    }
}